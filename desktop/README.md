# EMWaver Native Desktop (Slint)

Work-in-progress native desktop app using Slint.

This is intentionally developed alongside the existing Tauri desktop app in `app/`.

Dev run:

```bash
cargo run
```
