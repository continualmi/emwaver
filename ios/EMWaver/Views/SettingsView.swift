import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("Settings View Content")
            .navigationTitle("Settings")
    }
}

#Preview {
    NavigationView {
        SettingsView()
    }
} 