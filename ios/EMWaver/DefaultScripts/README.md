# Default Wavelet Scripts

These scripts are synced into the iOS app bundle at build time from the canonical desktop directory:

- `app/public/wavelet-assets/*.js`

See the Xcode build phase `Sync Wavelet Assets` in `ios/EMWaver.xcodeproj`.

