# Python dependencies (repo-wide)
#
# Includes:
# - backend/ (Flask API + cloud sync)
# - scripts/gen_image_gemini.py (Gemini image generation helper)

Flask==3.0.0
flask-cors==4.0.0
requests==2.31.0
gunicorn==21.2.0

# Storage
SQLAlchemy==2.0.46

# Postgres driver (Azure Postgres in prod; required if DATABASE_URL is Postgres)
# psycopg3 installs cleanly on modern Python (incl. 3.13) without pg_config.
psycopg[binary]==3.2.13

# Azure Blob Storage (signals/scripts/files)
azure-storage-blob==12.19.0

# Auth (Firebase ID token verification)
google-auth==2.26.2

# Dev-only utility deps
google-genai
pillow
