<Page
    x:Class="EMWaver.Pages.RemoteHostControlPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EMWaver.Pages"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <Grid Padding="16" RowSpacing="12" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
            <Button Content="Back" Click="OnBackClick" />
            <TextBlock x:Name="TitleText" Text="Remote Control" FontSize="20" FontWeight="SemiBold" />
            <TextBlock x:Name="StatusText" Text="" Opacity="0.75" VerticalAlignment="Center" />
        </StackPanel>

        <Border Grid.Row="1" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="12">
            <StackPanel Spacing="8">
                <TextBlock Text="Run on host (v1 dev UI)" Opacity="0.75" />
                <TextBox x:Name="RunNameBox" Header="Name" Text="remote.emw" />
                <TextBox x:Name="RunSourceBox" Header="Source" AcceptsReturn="True" TextWrapping="Wrap" Height="120" FontFamily="Consolas" FontSize="12" />
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button x:Name="ConnectButton" Content="Connect" Click="OnConnectClick" />
                    <Button x:Name="RunButton" Content="Run" Click="OnRunClick" />
                    <TextBlock x:Name="AttachText" Text="" Opacity="0.75" VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock x:Name="ErrorText" Text="" Foreground="{ThemeResource AppErrorTextBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="12">
            <Grid>
                <ScrollViewer>
                    <StackPanel x:Name="UiHost" />
                </ScrollViewer>
                <TextBlock x:Name="UiHint" Text="No remote UI yet. Connect + run." Opacity="0.75" VerticalAlignment="Center" HorizontalAlignment="Center" />
            </Grid>
        </Border>

    </Grid>
</Page>
