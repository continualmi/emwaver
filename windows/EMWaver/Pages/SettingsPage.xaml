<Page
    x:Class="EMWaver.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EMWaver.Pages"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <ScrollViewer>
        <StackPanel Padding="16" Spacing="12" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">

            <TextBlock Text="Settings" FontSize="20" FontWeight="SemiBold" />

            <Border Visibility="Collapsed"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Appearance" FontWeight="SemiBold" />

                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <TextBlock Text="Mode:" />
                        <ComboBox x:Name="ThemeModeCombo" SelectionChanged="OnThemeModeChanged" Width="220" Foreground="{ThemeResource TextFillColorPrimaryBrush}">
                            <ComboBoxItem Content="System" Tag="system" />
                            <ComboBoxItem Content="Light" Tag="light" />
                            <ComboBoxItem Content="Dark" Tag="dark" />
                        </ComboBox>
                    </StackPanel>

                    <TextBlock Text="System is the default and follows the device appearance setting."
                               Opacity="0.75"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Border>


            <Border BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Agent" FontWeight="SemiBold" />
                    <TextBlock x:Name="AgentKeyStatusText" Text="Checking Agent key…" Opacity="0.75" TextWrapping="Wrap" />
                    <TextBlock Text="Agent replies use a user-provided API key. Local scripts and local hardware control do not require it."
                               Opacity="0.75"
                               TextWrapping="Wrap" />
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Configure Agent Key…" Click="OnConfigureAgentKeyClick" />
                        <Button Content="Refresh" Click="OnRefreshAgentKeyClick" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Local browser control" FontWeight="SemiBold" />
                    <ToggleSwitch x:Name="LocalGatewayToggle"
                                  Header="Allow localhost gateway control"
                                  Toggled="OnLocalGatewayToggled" />
                    <TextBlock Text="When enabled, this app accepts script and UI control from the local EMWaver gateway on this PC. Leave it off unless you are using the browser control surface."
                               Opacity="0.75"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
