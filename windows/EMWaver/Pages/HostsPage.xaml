<Page
    x:Class="EMWaver.Pages.HostsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EMWaver.Pages"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <ScrollViewer>
        <StackPanel Padding="16" Spacing="12" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">

            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <Button Content="Back" Click="OnBackClick" />
                <TextBlock Text="Hosts" FontSize="20" FontWeight="SemiBold" />
                <Button Content="Refresh" Click="OnRefreshClick" />
                <Button x:Name="GetProButton" Content="Get EMWaver Pro…" Click="OnGetProClick" Visibility="Collapsed" />
            </StackPanel>

            <TextBlock x:Name="StatusText" Text="" Opacity="0.75" TextWrapping="Wrap" />

            <Border x:Name="ProBanner"
                    Visibility="Collapsed"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Remote host sessions are an EMWaver Pro feature." FontWeight="SemiBold" />
                    <TextBlock Text="You can browse host presence, but controlling a host is locked until you upgrade."
                               Opacity="0.75"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <ListView x:Name="HostsList" SelectionMode="None">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="12"
                                Margin="0,0,0,10"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                            <StackPanel Spacing="6">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="Control" Click="OnControlClick" Tag="{Binding HostSessionId}" IsEnabled="{Binding CanControl}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                    <Ellipse Width="8" Height="8">
                                        <Ellipse.Fill>
                                            <SolidColorBrush Color="{Binding OnlineColor}" />
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding UsbLabel}" Opacity="0.75" />
                                </StackPanel>

                                <TextBlock Text="{Binding Subtitle}" Opacity="0.75" />
                                <TextBlock Text="{Binding PortLine}" Opacity="0.75" />
                                <TextBlock Text="{Binding ScriptLine}" Opacity="0.75" />
                                <TextBlock Text="{Binding IdLine}" Opacity="0.60" FontSize="12" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackPanel>
    </ScrollViewer>
</Page>
