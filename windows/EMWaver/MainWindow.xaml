<Window
    x:Class="EMWaver.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EMWaver"
    Title="EMWaver" >

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="EMWaver (Windows 11)"
            FontSize="20"
            FontWeight="SemiBold" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Margin="0,12,0,12">
            <Button Content="Self-test buffer core" Click="OnSelfTestClick" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Text="Not started" />
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <TextBlock x:Name="LogText" TextWrapping="Wrap" FontFamily="Consolas" />
        </ScrollViewer>
    </Grid>
</Window>
