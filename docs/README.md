# EMWaver Docs

MkDocs Material site:
- Source: `docs/content/`
- Config: `docs/mkdocs.yml`
- Build output (GitHub Pages): `docs/docs/`

## Local preview

```bash
python -m pip install -r docs/requirements.txt
python -m mkdocs serve -f docs/mkdocs.yml
```
