# CLI Releases (Deprecated)

EMWaver does **not** ship a CLI.

`cli/` is **internal/dev-only** tooling and should not have public releases or installers.

If you need the CLI for development, build it from source:

```bash
cd cli
cargo build --release
```
