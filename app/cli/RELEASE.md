# CLI Releases (Deprecated)

EMWaver does **not** ship a CLI.

`app/cli/` is **internal/dev-only** tooling and should not have public releases or installers.

If you need the CLI for development, build it from source:

```bash
cd app/cli
cargo build --release
```
