# Legacy Desktop App (Tauri) - Observation Only

`app/` contains a legacy Tauri/React desktop app.

This is **observation-only**:

- Do not add new product features here.
- Do not publish releases from this app.
- Desktop product work should land in the native apps (macOS: `macos/`, Windows: TBD).

Shared Rust crates live under `crates/`.
Internal CLI tooling lives under `app/cli/`.
