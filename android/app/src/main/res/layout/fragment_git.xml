<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp"
    android:background="@color/surfaceBackground"
    tools:context=".ui.git.GitFragment">

    <!-- Login State -->
    <LinearLayout
        android:id="@+id/login_state"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/login_button"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Connect with GitHub"
            android:textSize="18sp"
            android:paddingStart="32dp"
            android:paddingEnd="32dp"
            android:paddingTop="16dp"
            android:paddingBottom="16dp" />

    </LinearLayout>

    <!-- Authenticated State -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/authenticated_state"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Repository List Card -->
        <androidx.cardview.widget.CardView
            android:id="@+id/repo_list_card"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:maxHeight="200dp"
            app:cardElevation="2dp"
            app:cardCornerRadius="4dp"
            app:cardBackgroundColor="@color/surfaceCard"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <ListView
                android:id="@+id/repo_list_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/surfaceCard" />
        </androidx.cardview.widget.CardView>

        <!-- File Tree Card -->
        <androidx.cardview.widget.CardView
            android:id="@+id/file_tree_card"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:maxHeight="200dp"
            android:layout_marginTop="8dp"
            app:cardElevation="2dp"
            app:cardCornerRadius="4dp"
            app:cardBackgroundColor="@color/surfaceCard"
            app:layout_constraintTop_toBottomOf="@id/repo_list_card"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <ListView
                android:id="@+id/file_tree_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/surfaceCard" />
        </androidx.cardview.widget.CardView>

        <!-- File Editor Container -->
        <FrameLayout
            android:id="@+id/file_editor_container"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone"
            android:background="@color/surfaceBackground"
            app:layout_constraintTop_toBottomOf="@id/file_tree_card"
            app:layout_constraintBottom_toTopOf="@id/commit_button_container"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <HorizontalScrollView
                android:id="@+id/editor_scroll_view_nowrap"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:fillViewport="true"
                android:scrollbars="horizontal">

                <EditText
                    android:id="@+id/file_editor_content"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:minWidth="280dp"
                    android:gravity="top|start"
                    android:fontFamily="monospace"
                    android:padding="16dp"
                    android:inputType="textMultiLine|textNoSuggestions"
                    android:scrollHorizontally="true"
                    android:textColor="@color/codeText"
                    android:textColorHint="@color/textSecondary"
                    android:textSize="12sp" />
            </HorizontalScrollView>
        </FrameLayout>

        <!-- Commit Button Container -->
        <LinearLayout
            android:id="@+id/commit_button_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end"
            android:padding="8dp"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/commit_button"
                style="@style/Widget.MaterialComponents.Button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Commit Changes"
                android:layout_marginEnd="8dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/cancel_edit_button"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Cancel" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
